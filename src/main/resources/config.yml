none:
  permissions:

member:
  permissions:

novice:
  permissions:

master:
  permissions:

elite:
  permissions:

rt:
  permissions:
    - plots.admin.command.kick
    - creative.rankup
    - creative.randown

ls:
  permissions:
    - plots.admin.command.kick
    - creative.rankup
    - creative.rankdown
